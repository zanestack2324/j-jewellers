{
  "name": "j-jewellers",
  "version": "1.1.0",
  "private": true,
  "description": "J Jewellers - Look Boujee On A Budget. Handcrafted luxury jewellery e-commerce store.",
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "sharp": "^0.35.3",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "puppeteer-core": "^25.3.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1",
    "health": "curl -s http://localhost:3000/api/health | node -e \"process.stdin.on('data',d=>console.log(JSON.parse(d)))\""
  }
}
